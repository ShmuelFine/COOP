#include "Counter.h"


FUN_IMPL(main, void)

   /* init_global_memory(0, HEAP_BASED_MEMORY);
    
    SCOPE_START;

    CREATE(Counter, c),3 CALL;
    MFUN(&c, Inc) CALL;
    MFUN(&c, Add) CALL(2);

    int val = -1;
    MFUN(&c, Get) CALL(&val);

    printf("COUNTER value = %d\n", val);

    SCOPE_END*/
    RETURN;
 END_FUN
    
